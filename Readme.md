MODULES ES6 - Simply put, Module is a single statement placed in different files

1. Provide Structure
2. Split Code Into Smaller Files
3. Share Code Across App
4. Same as React
5. Use Server Extension Locally
6. Used in Projects

And our next topic is going to be ES6 modules.

And I know what you're thinking.

So if the modules come with ES6 and we just covered ES6, why we have a separate topic for modules and

it's quite simple since we're going to use modules for the remaining of the course and I personally

like them so much, I decided to treat them in a special way and place them in a separate section.

What is a module is just a file.

In its simplest form, you can think of a module as one statement placed in a separate file.

For example, you can have a console log placed in a separate file and then reach for that file any

time you need to run console log in your program.

What are the benefits of the modules and why?

I like them so much because they allow us to split up our code into smaller files.

Share that code however we please, and as a result, it's far easier to manage our app since we don't

have to navigate one giant app.js file.

Initially modules were lacking browser support, but at the time of this recording they are supporting

more than 90% browser support and it's only going to get better.

So I think we are in good shape.

React also uses the same module system, so it's not like you're wasting your time.

In fact, it's the opposite.

By the time you'll get to react, you'll be already familiar with one of the major building blocks of

React application.

I do want to mention though, in order to work with modules locally, you must, must, must, must,

must use one of the local server extensions that I mentioned previously.

Since you will get an error if you use just a regular file system.

Now, once we deploy our app up on internet, we don't really care since it's going to be on the server

anyway.

But locally only server extensions are a way to develop app with modules.

Finally, since we're going to use modules in most of our future projects and sections, I'm not planning

on spending three hours on covering every smallest detail.

My plan is to cover basics so we can get up and running with modules in no time.

And as we're progressing with the course, we'll learn some additional things as well.
